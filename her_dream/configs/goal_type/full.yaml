# @package _global_
goal_type: full
state_repr: stoch # stoch | dist | logit | prob   (reward fn's state arg)
goal_repr: sample # sample | argmax | logit | prob (the goal tensor)
scope: full # first_row | full              (actor/critic goal input)
uses_threshold: false
