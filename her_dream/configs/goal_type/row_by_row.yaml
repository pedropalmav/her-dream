# @package _global_
goal_type: row_by_row
state_repr: stoch # stoch | dist | logit   (reward fn's state arg)
goal_repr: sample # sample | argmax | logit (the goal tensor)
scope: full # first_row | full          (actor/critic goal input)
uses_threshold: false
# step cost; reward is offset + matching_rows/S, so -1 gives the historical [-1, 0].
# Set to 0.0 on the -1-based types for a non-negative scheme (see rewards.py).
reward_offset: -1.0
